# image-load-n-flop
download (or read locally) image and generate flipped (both horizontally &amp; vertically) version of it

run tests
```
npm run test
npm run lint
```

run build
```
npm run build
```

